#include <stdio.h>
#include <ctype.h>

int main()
{
    char ch;

    printf("Enter source code (Press Ctrl+Z to stop):\n");

    while((ch=getchar())!=EOF)
    {
        if(ch==' ' || ch=='\t' || ch=='\n')
            continue;

        printf("%c", ch);
    }

    return 0;
}
